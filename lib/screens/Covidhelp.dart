import 'package:flutter/material.dart';

class Covidhelp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color.fromRGBO(28, 27, 37, 1),
      body: Container(
        child: Column(
          children: [

          ],
        ),
      ),
    );
  }
}
